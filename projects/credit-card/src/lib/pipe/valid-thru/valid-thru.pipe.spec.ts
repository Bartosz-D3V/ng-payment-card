import { ValidThruPipe } from './valid-thru.pipe';

describe('ValidThruPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidThruPipe();
    expect(pipe).toBeTruthy();
  });
});
